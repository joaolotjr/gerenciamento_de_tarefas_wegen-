# gerenciamento_de_tarefas_wegen-
Desenvolvimento de uma aplicação de gerenciamento de tarefas (To-Do List) que permita criar, ler, atualizar, deletar e categorizar tarefas.

# To-Do List Full Stack (ASP.NET Core + React)

### 🚀 Como rodar o Backend
1. Entre na pasta `TodoApi`.
2. Configure sua string de conexão do PostgreSQL no `appsettings.json`.
3. Execute `dotnet ef database update` para criar as tabelas.
4. Execute `dotnet run`.

### 💻 Como rodar o Frontend
1. Entre na pasta `todo-web`.
2. Execute `npm install`.
3. Execute `npm run dev`.

### 🛠 Tecnologias
- **Backend:** C# (ASP.NET Core 8), Entity Framework Core, PostgreSQL, JWT, BCrypt.
- **Frontend:** React, TypeScript, Vite, Material-UI, Axios.
